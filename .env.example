# Supabase (opcional — sem isso o app usa só localStorage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# E-mail admin (vê tabela de usuários em Config)
NEXT_PUBLIC_ADMIN_EMAIL=

# GitHub Pages (/appfinanceiro) — vazio no dev local
NEXT_PUBLIC_BASE_PATH=
